using LiteScript;
using LiteScript.Frontend;
using PuzzlePlatformer.litescript.Frontend;
using PuzzlePlatformer.litescript.Runtime;
/*
var parser = new Parser();
var interpreter = new Interpreter();
var env = Env.CreateGlobalEnvironment();

var input = File.ReadAllText("/home/shaun/Documents/Programming/C#/LiteScript/LiteScript/TestFile.txt");

var script = parser.ProduceAst(input);

var result = interpreter.Evaluate(script, env);
// Console.WriteLine(result);

Environment.Exit(0); */